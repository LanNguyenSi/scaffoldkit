# Application Placeholder

Place the Symfony application source code for `{{ display_name }}` in this directory.

The surrounding repository already provides Docker, CI, and quality-tooling scaffolding. This directory exists because `docker-compose.yml` mounts `./project` into the runtime containers.
