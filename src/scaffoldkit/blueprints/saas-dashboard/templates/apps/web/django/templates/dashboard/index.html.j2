<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ display_name }}</title>
    <link rel="stylesheet" href="/static/app.css" />
  </head>
  <body>
    <main>
      <h1>{{ display_name }}</h1>
      <p>{{ description }}</p>
      <p>HTMX-ready server-rendered dashboard bootstrap.</p>
    </main>
  </body>
</html>
