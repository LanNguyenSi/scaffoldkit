export default function DashboardPage() {
  return (
    <main>
      <h1>{{ display_name }}</h1>
      <p>{{ description }}</p>
      <p>Frontend bootstrap for the {{ stack }} stack.</p>
    </main>
  );
}
