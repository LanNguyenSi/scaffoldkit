import express from "express";

const app = express();
const port = Number(process.env.PORT ?? 8080);

app.get("/health", (_req, res) => {
  res.json({ status: "ok", service: "{{ project_name }}-api" });
});

app.listen(port, () => {
  console.log(`API listening on http://localhost:${port}`);
});
