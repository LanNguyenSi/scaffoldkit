{% if db_provider == 'postgresql' %}DATABASE_URL="postgresql://dev:dev@localhost:5432/{{ project_name }}_dev?schema=public"
{% elif db_provider == 'mysql' %}DATABASE_URL="mysql://dev:dev@localhost:3306/{{ project_name }}_dev"
{% else %}DATABASE_URL="file:./dev.db"
{% endif %}{% if auth_strategy == 'jwt' %}
# Secret used to sign JWT access tokens. Generate one with: openssl rand -base64 32
JWT_SECRET="change-me"
{% endif %}